```
pip install -r requirements.txt
```

To run the tool:

```
python3 main.py --security-attack-scan --file-path '{file_path}' --repo-path '{repo_path}'
```

To run tool with different t value, please go to t_ary_testing.py and mofify the t value:

```
T_WAY_PAIR_WISE_TEST = 4
```
